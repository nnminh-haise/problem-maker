
# Các bước tạo bài tập:

- Bước 1: Tìm đề
- Bước 2: Giải đề (bằng `cpp` vào trong file `solution.cpp`).
- Bước 3: Dựa vào dữ liệu đầu vào (input) của đề và giới hạn của đề để viết trình sinh dữ liệu vào (`generator.cpp`) $\rightarrow$ test file `generator.cpp`.
- Bước 4: Chạy file `creator.py`

